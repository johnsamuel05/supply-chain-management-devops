pipeline {
    agent any
    
    environment {
        DOCKERFILE_PATH = "inventory_service/Dockerfile" // Relative path to Dockerfile
        IMAGE_NAME = "inventory-service:${env.BUILD_NUMBER}"
    }

    stages {
        stage('Build') {
            steps {
                script {
                    // Build Docker image using Dockerfile in a different directory
                    docker.build(IMAGE_NAME, "-f ${DOCKERFILE_PATH} .")
                }
            }
        }
    }

        stage('Test') {
            steps {
                script {
                    docker.image(IMAGE_NAME).inside {
                        sh 'pytest'
                    }
                }
            }
        }

        stage('Push to Docker Registry') {
            steps {
                script {
                    docker.withRegistry('https://index.docker.io/v1/', 'dockerhub_id') {
                        docker.image(IMAGE_NAME).push()
                    }
                }
            }
        }
    }
}
