pipeline {
    agent any
    
    environment {
        DOCKERFILE_PATH = "/home/sam/Documents/app/inventory_service/Dockerfile"
        IMAGE_NAME = "inventory-service:${env.BUILD_NUMBER}"
    }

    stages {
        stage('Build') {
            steps {
                script {
                    docker.build(IMAGE_NAME, "-f ${DOCKERFILE_PATH} /home/sam/Documents/app/inventory_service")
                }
            }
        }

        stage('Test') {
            steps {
                script {
                    docker.image(IMAGE_NAME).inside {
                        sh 'pytest'
                    }
                }
            }
        }

        stage('Push to Docker Registry') {
            steps {
                script {
                    docker.withRegistry('https://index.docker.io/v1/', 'dockerhub_id') {
                        docker.image(IMAGE_NAME).push()
                    }
                }
            }
        }
    }
}
