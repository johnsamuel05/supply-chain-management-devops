pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                script {
                    docker.build("delivery-service:${env.BUILD_NUMBER}")
                }
            }
        }
        stage('Test') {
            steps {
                script {
                    docker.image("delivery-service:${env.BUILD_NUMBER}").inside {
                        sh 'pytest'
                    }
                }
            }
        }
        stage('Push to Docker Registry') {
            steps {
                script {
                    docker.withRegistry('https://index.docker.io/v1/', 'dockerhub_id') {
                        docker.image("delivery-service:${env.BUILD_NUMBER}").push()
                    }
                }
            }
        }
    }
}
